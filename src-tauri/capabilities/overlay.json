{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay-capability",
  "description": "Minimal capability for screenshot overlay windows",
  "windows": [
    "screenshot-overlay-*"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-close"
  ]
}
